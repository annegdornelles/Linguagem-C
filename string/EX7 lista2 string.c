/*Faça um programa que lê uma linha de texto em uma string (sequência de caracteres terminada por '\0')
e que conta o número de ocorrências de cada letra do alfabeto.*/
#include<stdio.h>
#include<string.h>
main (){
    char [150];
    int i,cont;

    printf ("Insira uma string:");
    gets (str);

    tam = strlen (str);

    for (i=0;i<tam;i++){
        if (str[1]==str[i]){
            cont++;
        }
    }

}
